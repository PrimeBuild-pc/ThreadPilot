<ui:FluentWindow x:Class="ThreadPilot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ThreadPilot"
        xmlns:views="clr-namespace:ThreadPilot.Views"
        xmlns:viewModels="clr-namespace:ThreadPilot.ViewModels"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Title="{DynamicResource MainWindow_Title}"
        Height="864"
        Width="1280"
        WindowStartupLocation="CenterScreen"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        KeyboardNavigation.TabNavigation="Cycle"
        KeyboardNavigation.ControlTabNavigation="Cycle">

    <ui:FluentWindow.Resources>
        <!-- Loading Spinner Animation -->
        <Storyboard x:Key="SpinnerAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="SpinnerRotation"
                           Storyboard.TargetProperty="Angle"
                           From="0" To="360" Duration="0:0:1"/>
        </Storyboard>

        <!-- Fade In Animation -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetName="LoadingOverlay"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.2"/>
        </Storyboard>

        <!-- Fade Out Animation -->
        <Storyboard x:Key="FadeOutAnimation">
            <DoubleAnimation Storyboard.TargetName="LoadingOverlay"
                           Storyboard.TargetProperty="Opacity"
                           From="1" To="0" Duration="0:0:0.3"/>
        </Storyboard>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid x:Name="UIContent">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=PerformanceIntroOverlay, Path=Visibility}" Value="Visible">
                            <Setter Property="IsHitTestVisible" Value="False"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <BlurEffect Radius="8" KernelType="Gaussian"/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=UnsavedSettingsOverlay, Path=Visibility}" Value="Visible">
                            <Setter Property="IsHitTestVisible" Value="False"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <BlurEffect Radius="8" KernelType="Gaussian"/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="ThreadPilot">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/assets/icons/ico.ico"/>
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1">
            <!-- Main Application Content (NavigationView) -->
            <ui:NavigationView x:Name="RootNavigation"
                               PaneDisplayMode="Left"
                               IsBackButtonVisible="Collapsed"
                               IsPaneOpen="False"
                               OpenPaneLength="180"
                               CompactPaneLength="52"
                               AutomationProperties.Name="{DynamicResource MainWindow_PrimaryNavigation}">
                <ui:NavigationView.Resources>
                    <Style TargetType="ui:NavigationViewItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Padding" Value="12,4"/>
                        <Setter Property="MinHeight" Value="36"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource QuietRowBackgroundBrush}"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}"/>
                            </Trigger>
                            <Trigger Property="IsActive" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SoftSelectionBackgroundBrush}"/>
                                <Setter Property="BorderThickness" Value="3,0,0,0"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentFillColorDefaultBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- Remove default NavigationViewItem horizontal separator lines -->
                    <Style x:Key="NavigationViewItemSeparator" TargetType="Rectangle">
                        <Setter Property="Fill" Value="Transparent"/>
                        <Setter Property="Height" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                    </Style>
                </ui:NavigationView.Resources>
                
                <ui:NavigationView.MenuItems>
                    <ui:NavigationViewItem x:Name="NavProcess" Content="{DynamicResource MainWindow_NavProcess}" Tag="Process" TargetPageTag="Process" Click="NavMenuItem_Click">
                        <ui:NavigationViewItem.Icon>
                            <ui:SymbolIcon Symbol="Apps24"/>
                        </ui:NavigationViewItem.Icon>
                    </ui:NavigationViewItem>
                    <ui:NavigationViewItem x:Name="NavMasks" Content="{DynamicResource MainWindow_NavMasks}" Tag="Masks" TargetPageTag="Masks" Click="NavMenuItem_Click">
                        <ui:NavigationViewItem.Icon>
                            <ui:SymbolIcon Symbol="BranchFork24"/>
                        </ui:NavigationViewItem.Icon>
                    </ui:NavigationViewItem>
                    <ui:NavigationViewItem x:Name="NavPower" Content="{DynamicResource MainWindow_NavPower}" Tag="Power" TargetPageTag="Power" Click="NavMenuItem_Click">
                        <ui:NavigationViewItem.Icon>
                            <ui:SymbolIcon Symbol="Power24"/>
                        </ui:NavigationViewItem.Icon>
                    </ui:NavigationViewItem>
                    <ui:NavigationViewItem x:Name="NavRules" Content="{DynamicResource MainWindow_NavRules}" Tag="Rules" TargetPageTag="Rules" Click="NavMenuItem_Click">
                        <ui:NavigationViewItem.Icon>
                            <ui:SymbolIcon Symbol="TasksApp24"/>
                        </ui:NavigationViewItem.Icon>
                    </ui:NavigationViewItem>
                    <ui:NavigationViewItem x:Name="NavPerf" Content="{DynamicResource MainWindow_NavDiagnostics}" Tag="Performance" TargetPageTag="Performance" Click="NavMenuItem_Click">
                        <ui:NavigationViewItem.Icon>
                            <ui:SymbolIcon Symbol="DataHistogram24"/>
                        </ui:NavigationViewItem.Icon>
                    </ui:NavigationViewItem>
                    <ui:NavigationViewItem x:Name="NavLogs" Content="{DynamicResource MainWindow_NavActivity}" Tag="Logs" TargetPageTag="Logs" Click="NavMenuItem_Click">
                        <ui:NavigationViewItem.Icon>
                            <ui:SymbolIcon Symbol="DocumentText24"/>
                        </ui:NavigationViewItem.Icon>
                    </ui:NavigationViewItem>
                    <ui:NavigationViewItem x:Name="NavTweaks" Content="{DynamicResource MainWindow_NavTweaks}" Tag="Tweaks" TargetPageTag="Tweaks" Click="NavMenuItem_Click">
                        <ui:NavigationViewItem.Icon>
                            <ui:SymbolIcon Symbol="Wrench24"/>
                        </ui:NavigationViewItem.Icon>
                    </ui:NavigationViewItem>
                </ui:NavigationView.MenuItems>
                
                <ui:NavigationView.FooterMenuItems>
                    <ui:NavigationViewItem x:Name="NavSettings" TargetPageTag="Settings" Content="{DynamicResource MainWindow_NavSettings}" Tag="Settings" Click="NavMenuItem_Click">
                        <ui:NavigationViewItem.Icon>
                            <ui:SymbolIcon Symbol="Settings24"/>
                        </ui:NavigationViewItem.Icon>
                    </ui:NavigationViewItem>
                </ui:NavigationView.FooterMenuItems>

                <ui:NavigationView.ContentOverlay>
                    <Grid x:Name="MainContent" Margin="12,0,12,12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                    
                    <!-- Elevation Warning Modal (Shell Login Style) -->
                    <Grid x:Name="ElevationWarningOverlay"
                          Grid.RowSpan="2"
                          Visibility="Collapsed"
                          Panel.ZIndex="900"
                          AutomationProperties.Name="{DynamicResource MainWindow_ElevationWarningOverlay}">
                        <!-- Blurred Backdrop -->
                        <Grid x:Name="ElevationWarningBackdrop"
                              Background="{DynamicResource OverlayBrush}"
                              IsHitTestVisible="True">
                            <Grid.Effect>
                                <BlurEffect x:Name="ElevationWarningBlur" Radius="0"/>
                            </Grid.Effect>
                        </Grid>

                        <!-- Shell Login Popup -->
                        <Border x:Name="ElevationWarningCard"
                                Background="{DynamicResource CardSurfaceBrush}"
                                BorderBrush="{DynamicResource BorderSubtleBrush}"
                                BorderThickness="1"
                                CornerRadius="{DynamicResource StandardCardCornerRadius}"
                                Padding="24"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                MinWidth="420"
                                MaxWidth="560"
                                IsHitTestVisible="True">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.18"/>
                            </Border.Effect>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                    <TextBlock Text="⚠️"
                                               FontSize="28"
                                               VerticalAlignment="Center"
                                               Margin="0,0,12,0"/>
                                    <TextBlock Text="{DynamicResource MainWindow_ElevationWarningTitle}"
                                               FontSize="22"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                               VerticalAlignment="Center"
                                               TextWrapping="Wrap"/>
                                </StackPanel>

                                <TextBlock Text="{DynamicResource MainWindow_ElevationWarningDescription}"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,12"
                                           FontSize="14"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

                                <TextBlock Text="{DynamicResource MainWindow_ElevationWarningRequired}"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>

                                <StackPanel Margin="20,0,0,0">
                                    <TextBlock Text="{DynamicResource MainWindow_ElevationWarningPowerPlans}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,4"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{DynamicResource MainWindow_ElevationWarningAffinity}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,4"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{DynamicResource MainWindow_ElevationWarningTweaks}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,4"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{DynamicResource MainWindow_ElevationWarningProtected}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,16"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="{DynamicResource MainWindow_ContinueWithoutElevation}"
                                            Click="ElevationWarningDismiss_Click"
                                            Margin="0,0,12,0"
                                            Background="{DynamicResource ControlFillColorDefaultBrush}"
                                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                            Padding="16,8"/>
                                    <Button Content="{DynamicResource MainWindow_RequestElevation}"
                                            Command="{Binding RequestElevationCommand}"
                                            Click="ElevationWarningRequestElevation_Click"
                                            Background="{DynamicResource AccentFillColorDefaultBrush}"
                                            Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"
                                            Padding="16,8"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Hosted Views toggled natively to retain ViewModel mapping -->
                    <Grid Grid.Row="1">
                        <views:ProcessView x:Name="ProcessManagementTab" Visibility="Visible"/>
                        <views:MasksView x:Name="CoreMasksTab" Visibility="Collapsed"/>
                        <views:PowerPlanView x:Name="PowerPlanViewControl" Visibility="Collapsed"/>
                        <views:ProcessPowerPlanAssociationView x:Name="AssociationView" Visibility="Collapsed"/>
                        <views:PerformanceView x:Name="PerformanceViewControl" Visibility="Collapsed"/>
                        <views:LogViewerView x:Name="LogViewerViewControl" Visibility="Collapsed"/>
                        <views:SystemTweaksView x:Name="SystemTweaksView" Visibility="Collapsed"/>
                        <views:SettingsView x:Name="SettingsView" Visibility="Collapsed"/>
                    </Grid>
                </Grid>
                </ui:NavigationView.ContentOverlay>
            </ui:NavigationView>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{DynamicResource SolidBackgroundFillColorBaseBrush}" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderSubtleBrush}" Padding="12,4">
            <Border.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
                    <Setter Property="FontSize" Value="12"/>
                </Style>
            </Border.Resources>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center" Opacity="{Binding StatusOpacity}"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding ElevationStatusText}"/>
                    <TextBlock Text="🔒"
                               Margin="5,0,0,0"
                               Visibility="{Binding IsRunningAsAdministrator, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock Text=" • " Margin="8,0,8,0" Opacity="0.7"/>
                    <TextBlock Text="{Binding ApplicationVersion}" Opacity="0.85"/>
                </StackPanel>
            </Grid>
        </Border>
        </Grid>

        <Grid x:Name="PopupOverlay"
              Panel.ZIndex="950">
            <Grid x:Name="PerformanceIntroOverlay"
                  Visibility="Collapsed"
                  AutomationProperties.Name="{DynamicResource MainWindow_PerformanceIntroductionOverlay}">
                <Border x:Name="PerformanceIntroCard"
                        Background="{DynamicResource CardSurfaceBrush}"
                        BorderBrush="{DynamicResource BorderSubtleBrush}"
                        BorderThickness="1"
                        CornerRadius="{DynamicResource StandardCardCornerRadius}"
                        Padding="24"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        MinWidth="360"
                        MaxWidth="560"
                        Opacity="1">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.18"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock x:Name="PerformanceIntroTitle"
                                   Text="{DynamicResource PerformanceView_OptionalDiagnostics}"
                                   FontSize="24"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                   Margin="0,0,0,10"/>

                        <TextBlock x:Name="PerformanceIntroDescription"
                                   Text="{DynamicResource PerformanceView_DiagnosticsDesc}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,10"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>

                        <TextBlock x:Name="PerformanceIntroTipsHeader"
                                   Text="{DynamicResource PerformanceView_QuickTips}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                   Margin="0,0,0,6"/>

                        <TextBlock x:Name="PerformanceIntroTip1"
                                   Text="{DynamicResource PerformanceView_Tip1}"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Margin="0,0,0,4"/>
                        <TextBlock x:Name="PerformanceIntroTip2"
                                   Text="{DynamicResource PerformanceView_Tip2}"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Margin="0,0,0,4"/>
                        <TextBlock x:Name="PerformanceIntroTip3"
                                   Text="{DynamicResource PerformanceView_Tip3}"
                                   TextWrapping="Wrap"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Margin="0,0,0,16"/>

                        <Button x:Name="PerformanceIntroContinueButton"
                                Content="{DynamicResource PerformanceView_Continue}"
                                Click="PerformanceIntroContinue_Click"
                                HorizontalAlignment="Right"
                                Background="{DynamicResource AccentFillColorDefaultBrush}"
                                Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"
                                Padding="16,8"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid x:Name="StartupMinimizedSuggestionOverlay"
                  Visibility="Collapsed"
                  Panel.ZIndex="955"
                  Background="{DynamicResource OverlayBrush}"
                  AutomationProperties.Name="{DynamicResource MainWindow_StartupMinimizedSuggestion}">
                <Border Background="{DynamicResource CardSurfaceBrush}"
                        BorderBrush="{DynamicResource BorderSubtleBrush}"
                        BorderThickness="1"
                        CornerRadius="{DynamicResource StandardCardCornerRadius}"
                        Padding="20"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Top"
                        Margin="0,56,0,0"
                        MinWidth="420"
                        MaxWidth="560"
                        IsHitTestVisible="True">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.16"/>
                    </Border.Effect>
                    <StackPanel>
                        <Border Background="{DynamicResource InfoBackgroundBrush}"
                                BorderBrush="{DynamicResource InfoBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="{DynamicResource StandardCardCornerRadius}"
                                Padding="8,3"
                                HorizontalAlignment="Left"
                                Margin="0,0,0,10">
                            <TextBlock Text="{DynamicResource MainWindow_Recommended}"
                                       FontSize="{DynamicResource CaptionFontSize}"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource InfoTextBrush}"/>
                        </Border>
                        <TextBlock Text="{DynamicResource MainWindow_StartupMinimizedSuggestionTitle}"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="{DynamicResource MainWindow_StartupMinimizedSuggestionDescription}"
                                   TextWrapping="Wrap"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Margin="0,0,0,16"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="{DynamicResource MainWindow_DontShowAgain}"
                                    Click="StartupSuggestionDontShowAgain_Click"
                                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                    Padding="14,7"
                                    Margin="0,0,10,0"/>
                            <Button Content="{DynamicResource MainWindow_OpenSettings}"
                                    Click="StartupSuggestionOpenSettings_Click"
                                    Background="{DynamicResource AccentFillColorDefaultBrush}"
                                    Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"
                                    Padding="14,7"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid x:Name="UnsavedSettingsOverlay"
                  Visibility="Collapsed"
                  Panel.ZIndex="960"
                  Background="{DynamicResource OverlayBrush}"
                  AutomationProperties.Name="{DynamicResource MainWindow_UnsavedSettingsDialog}">
                <Border Background="{DynamicResource CardSurfaceBrush}"
                        BorderBrush="{DynamicResource BorderSubtleBrush}"
                        BorderThickness="1"
                        CornerRadius="{DynamicResource StandardCardCornerRadius}"
                        Padding="24"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        MinWidth="420"
                        MaxWidth="560"
                        IsHitTestVisible="True">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.18"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource SettingsWindow_UnsavedTitle}"
                                   FontSize="22"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                   Margin="0,0,0,10"/>
                        <TextBlock x:Name="UnsavedSettingsDialogMessage"
                                   TextWrapping="Wrap"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Margin="0,0,0,18"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="{DynamicResource SettingsWindow_Cancel}"
                                    Click="UnsavedSettingsCancel_Click"
                                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                    Padding="16,8"
                                    Margin="0,0,10,0"/>
                            <Button Content="{DynamicResource SettingsWindow_Discard}"
                                    Click="UnsavedSettingsDiscard_Click"
                                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                    Padding="16,8"
                                    Margin="0,0,10,0"/>
                            <Button Content="{DynamicResource SettingsWindow_Save}"
                                    Click="UnsavedSettingsSave_Click"
                                    Background="{DynamicResource AccentFillColorDefaultBrush}"
                                    Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"
                                    Padding="16,8"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Startup Loading Overlay -->
            <Grid x:Name="LoadingOverlay"
                  Visibility="Visible"
                  Panel.ZIndex="1000"
                  AutomationProperties.Name="{DynamicResource MainWindow_LoadingOverlay}"
                  AutomationProperties.LiveSetting="Polite">

            <!-- Loading Popup -->
            <Border Background="{DynamicResource CardSurfaceBrush}"
                    BorderBrush="{DynamicResource BorderSubtleBrush}"
                    BorderThickness="1"
                    CornerRadius="{DynamicResource StandardCardCornerRadius}"
                    Padding="30"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MinWidth="300"
                    MaxWidth="400"
                    IsHitTestVisible="True"
                    Opacity="1">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!-- Application Icon -->
                    <Border x:Name="LoadingIconBackground"
                            Background="{DynamicResource QuietRowBackgroundBrush}"
                            CornerRadius="{DynamicResource StandardCardCornerRadius}"
                            Padding="10"
                            Margin="0,0,0,20"
                            HorizontalAlignment="Center">
                        <Image x:Name="LoadingIcon"
                               Source="pack://application:,,,/assets/icons/ico.ico"
                               Width="64"
                               Height="64"
                               HorizontalAlignment="Center"/>
                    </Border>

                    <!-- Loading Spinner -->
                    <ui:ProgressRing IsIndeterminate="True" Margin="0,0,0,15" />

                    <TextBlock Text="{Binding InitializationStage}"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,10"/>

                    <TextBlock Text="{Binding InitializationDetails}"
                               FontSize="12"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap"
                               TextAlignment="Center"/>
                </StackPanel>
            </Border>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
