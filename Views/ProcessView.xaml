<UserControl x:Class="ThreadPilot.Views.ProcessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ThreadPilot.Converters"
             xmlns:Diagnostics="clr-namespace:System.Diagnostics;assembly=System.Diagnostics.Process"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             KeyboardNavigation.TabNavigation="Cycle"
             KeyboardNavigation.ControlTabNavigation="Cycle"
             d:DesignHeight="650" d:DesignWidth="1200">

    <UserControl.Resources>
        <converters:CoreTypeToColorConverter x:Key="CoreTypeToColorConverter"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:NullToBoolConverter x:Key="NullToBoolConverter"/>
        <converters:AffinityMaskConverter x:Key="AffinityMaskConverter"/>
        <converters:BytesToMbConverter x:Key="BytesToMbConverter"/>
        <sys:Double x:Key="ProcessFontLarge">24</sys:Double>
        <sys:Double x:Key="ProcessFontMedium">14</sys:Double>
        <sys:Double x:Key="ProcessFontMediumSmall">12</sys:Double>
        <sys:Double x:Key="ProcessFontSmall">11</sys:Double>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Process Management" Style="{StaticResource PageTitleTextStyle}" Margin="0,0,0,4"/>
            <TextBlock Text="Search, filter, and control active process configurations" Style="{StaticResource PageSubtitleTextStyle}"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0"/>
                <ColumnDefinition Width="0"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="{DynamicResource CardSurfaceBrush}"
                        Padding="{DynamicResource StandardCardPadding}"
                        BorderBrush="{DynamicResource BorderSubtleBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Search processes by name"
                                     HorizontalAlignment="Stretch"
                                     AutomationProperties.Name="Process search"/>
                            <CheckBox Grid.Column="2"
                                      Content="Active Apps Only"
                                      IsChecked="{Binding ShowActiveApplicationsOnly}"
                                      VerticalAlignment="Center"
                                      ToolTip="Show only applications with visible windows"/>
                            <Button Grid.Column="4"
                                    Command="{Binding LoadProcessesCommand}"
                                    Content="Refresh"
                                    ToolTip="Refresh the process list"
                                    AutomationProperties.Name="Refresh processes"/>
                            <Button Grid.Column="6"
                                    Command="{Binding LoadMoreProcessesCommand}"
                                    Content="Load More"
                                    Visibility="{Binding HasMoreBatches, Converter={StaticResource BoolToVisibilityConverter}}"
                                    ToolTip="Load more processes"
                                    AutomationProperties.Name="Load more processes"/>
                        </Grid>

                        <WrapPanel Grid.Row="2" Orientation="Horizontal">
                            <CheckBox Content="Hide System Processes"
                                      IsChecked="{Binding HideSystemProcesses}"
                                      Margin="0,0,15,0"
                                      VerticalAlignment="Center"
                                      ToolTip="Hide Windows system processes"/>
                            <CheckBox Content="Hide Idle Processes"
                                      IsChecked="{Binding HideIdleProcesses}"
                                      Margin="0,0,15,0"
                                      VerticalAlignment="Center"
                                      ToolTip="Hide processes with very low CPU usage"/>
                            <TextBlock Text="Sort by:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox SelectedValue="{Binding SortMode}"
                                      SelectedValuePath="Tag"
                                      Width="120"
                                      VerticalAlignment="Center"
                                      ToolTip="Choose how to sort the process list">
                                <ComboBoxItem Content="CPU Usage" Tag="CpuUsage"/>
                                <ComboBoxItem Content="Memory Usage" Tag="MemoryUsage"/>
                                <ComboBoxItem Content="Process Name" Tag="Name"/>
                                <ComboBoxItem Content="Process ID" Tag="ProcessId"/>
                            </ComboBox>
                        </WrapPanel>
                    </Grid>
                </Border>

                <Grid Grid.Row="1">
                    <DataGrid ItemsSource="{Binding FilteredProcesses}"
                              SelectedItem="{Binding SelectedProcess}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              CanUserResizeColumns="True"
                              CanUserSortColumns="True"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              VirtualizingPanel.ScrollUnit="Item"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              ScrollViewer.CanContentScroll="True"
                              AutomationProperties.Name="Running process list"
                              AutomationProperties.HelpText="Virtualized process table with sorting and selection">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="3,0,0,0"/>
                                <EventSetter Event="PreviewMouseRightButtonDown" Handler="ProcessRow_PreviewMouseRightButtonDown"/>
                                <Setter Property="ContextMenu">
                                    <Setter.Value>
                                        <ContextMenu DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                            <ContextMenu.Resources>
                                                <Style TargetType="MenuItem">
                                                    <Setter Property="FontWeight" Value="Normal"/>
                                                </Style>
                                            </ContextMenu.Resources>
                                            <MenuItem Header="Apply Affinity"
                                                      Command="{Binding ApplyContextAffinityCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <MenuItem Header="Clear CPU Sets"
                                                      Command="{Binding ClearContextCpuSetsCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <Separator/>
                                            <MenuItem Header="Rules">
                                                <MenuItem Header="Save Current Settings as Rule"
                                                          Command="{Binding SaveCurrentSettingsAsRuleCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="Apply Affinity and Save as Rule"
                                                          Command="{Binding ApplyAffinityAndSaveAsRuleCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            </MenuItem>
                                            <Separator/>
                                            <MenuItem Header="Set CPU Priority">
                                                <MenuItem Header="Below Normal"
                                                          Command="{Binding SetContextBelowNormalPriorityCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="Normal"
                                                          Command="{Binding SetContextNormalPriorityCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="Above Normal"
                                                          Command="{Binding SetContextAboveNormalPriorityCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="High"
                                                          Command="{Binding SetContextHighPriorityCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="Realtime (blocked)"
                                                          IsEnabled="False"
                                                          ToolTipService.ShowOnDisabled="True"
                                                          ToolTip="Realtime priority is blocked by ThreadPilot because it can make Windows unstable or unresponsive."/>
                                            </MenuItem>
                                            <MenuItem Header="Set Memory Priority">
                                                <MenuItem Header="Very Low"
                                                          Command="{Binding SetContextMemoryPriorityVeryLowCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="Low"
                                                          Command="{Binding SetContextMemoryPriorityLowCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="Medium"
                                                          Command="{Binding SetContextMemoryPriorityMediumCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="Below Normal"
                                                          Command="{Binding SetContextMemoryPriorityBelowNormalCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="Normal"
                                                          Command="{Binding SetContextMemoryPriorityNormalCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            </MenuItem>
                                            <Separator/>
                                            <MenuItem Header="Open Executable Location"
                                                      Command="{Binding OpenContextExecutableLocationCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <MenuItem Header="Copy Process Info"
                                                      Command="{Binding CopyContextProcessInfoCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <MenuItem Header="Refresh Process Info"
                                                      Command="{Binding RefreshContextProcessInfoCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        </ContextMenu>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource QuietRowHoverBackgroundBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource SoftSelectionBackgroundBrush}"/>
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="True"/>
                                            <Condition Property="IsKeyboardFocusWithin" Value="True"/>
                                        </MultiTrigger.Conditions>
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentAltBrush}"/>
                                    </MultiTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="160"/>
                            <DataGridTextColumn Header="ID" Binding="{Binding ProcessId}" Width="70"/>
                            <DataGridTextColumn Header="Window Title" Binding="{Binding MainWindowTitle}" Width="220"/>
                            <DataGridCheckBoxColumn Header="Has Window" Binding="{Binding HasVisibleWindow}" Width="90"/>
                            <DataGridTextColumn Header="CPU Usage" Binding="{Binding CpuUsage, StringFormat={}{0:N1}%}" Width="90"/>
                            <DataGridTextColumn Header="Memory (MB)"
                                                Binding="{Binding MemoryUsage, Converter={StaticResource BytesToMbConverter}}"
                                                Width="90"/>
                            <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Background="{DynamicResource OverlayBrush}" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <ProgressBar Value="{Binding LoadingProgress}" Maximum="100" Width="200" Height="20" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding LoadingStatusText}" HorizontalAlignment="Center" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <Border Background="{DynamicResource StatusPillBackgroundBrush}"
                            BorderBrush="{DynamicResource StatusPillBorderBrush}"
                            BorderThickness="1"
                            Visibility="{Binding IsVirtualizationEnabled, Converter={StaticResource BoolToVisibilityConverter}}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Margin="10"
                            Padding="8,5"
                            CornerRadius="{DynamicResource StandardCardCornerRadius}">
                        <TextBlock FontSize="{StaticResource ProcessFontSmall}" Foreground="{DynamicResource TextSecondaryBrush}">
                            <Run Text="Batch "/>
                            <Run Text="{Binding CurrentBatchIndex, StringFormat={}{0:N0}}"/>
                            <Run Text="/"/>
                            <Run Text="{Binding TotalBatches, StringFormat={}{0:N0}}"/>
                            <Run Text=" ("/>
                            <Run Text="{Binding TotalProcessCount, StringFormat={}{0:N0}}"/>
                            <Run Text=" total)"/>
                        </TextBlock>
                    </Border>
                </Grid>

                <Border Grid.Row="2"
                        Background="{DynamicResource CardSurfaceBrush}"
                        BorderBrush="{DynamicResource BorderSubtleBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="10"
                        Margin="0,12,0,0">
                    <StackPanel>
                        <TextBlock Text="Current process status"
                                   FontSize="{StaticResource ProcessFontMediumSmall}"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,2"/>
                        <TextBlock Text="Rules and changes are applied by ThreadPilot only when configured."
                                   FontSize="{StaticResource ProcessFontSmall}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,8"/>
                        <WrapPanel>
                            <TextBlock Text="{Binding SelectedProcessSummary.ProcessTitle}"
                                       FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis"
                                       Margin="0,0,18,4"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.CurrentProcessStatusText}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,4"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.CpuPriorityText}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,4"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.MemoryPriorityText}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,4"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.AffinityText}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,4"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.RuleStatusText}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,4"/>
                        </WrapPanel>
                        <TextBlock Text="{Binding SelectedProcessSummary.ExecutablePath}"
                                   FontSize="{StaticResource ProcessFontSmall}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   Margin="0,0,0,4"/>
                        <WrapPanel Margin="0,2,0,0">
                            <TextBlock Text="{Binding SelectedProcessSummary.CpuUsageText}"
                                       FontSize="{StaticResource ProcessFontSmall}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,0"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.MemoryUsageText}"
                                       FontSize="{StaticResource ProcessFontSmall}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,0"/>
                            <TextBlock FontSize="{StaticResource ProcessFontSmall}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       TextWrapping="Wrap">
                                <Run Text="Last ThreadPilot action: "/>
                                <Run Text="{Binding SelectedProcessSummary.LastOperationMessage, Mode=OneWay}"/>
                            </TextBlock>
                        </WrapPanel>

                        <Expander Header="Advanced affinity picker"
                                  IsExpanded="False"
                                  Margin="0,10,0,0">
                            <StackPanel Margin="0,8,0,0">
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="260"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="Pending core mask"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"/>
                                    <ComboBox Grid.Column="2"
                                              ItemsSource="{Binding AvailableCoreMasks}"
                                              SelectedItem="{Binding SelectedCoreMask, Mode=TwoWay}"
                                              DisplayMemberPath="Name"
                                              IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                              ToolTip="Select the pending mask for row context-menu affinity actions"/>
                                    <Button Grid.Column="4"
                                            Command="{Binding CreateCustomMaskCommand}"
                                            Padding="8,5"
                                            ToolTip="Open CPU Masks tab to create a new custom mask">
                                        <StackPanel Orientation="Horizontal">
                                            <ui:SymbolIcon Symbol="Add12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Custom" FontSize="{StaticResource ProcessFontMediumSmall}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>

                                <WrapPanel Margin="0,0,0,8">
                                    <TextBlock Text="{Binding CurrentAffinityText}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,0,18,4"/>
                                    <TextBlock Text="{Binding PendingAffinityText}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,0,18,4"/>
                                    <TextBlock Text="{Binding AffinityEditStateText}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{DynamicResource WarningTextBrush}"
                                               Margin="0,0,18,4"/>
                                </WrapPanel>

                                <WrapPanel Margin="0,0,0,6">
                                    <TextBlock Text="{Binding TopologyStatus}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{Binding IsTopologyDetectionSuccessful, Converter={StaticResource BoolToColorConverter}}"
                                               Margin="0,0,18,4"/>
                                    <TextBlock Text="{Binding HyperThreadingStatusText}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{Binding IsHyperThreadingActive, Converter={StaticResource BoolToColorConverter}}"
                                               Margin="0,0,18,4"/>
                                </WrapPanel>

                                <ScrollViewer MaxHeight="88"
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding CpuCores}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox Content="{Binding Label}"
                                                          IsChecked="{Binding IsSelected, Mode=OneWay}"
                                                          IsHitTestVisible="False"
                                                          Focusable="False"
                                                          IsEnabled="{Binding IsEnabled}"
                                                          Margin="2"
                                                          FontSize="{StaticResource ProcessFontSmall}"
                                                          Foreground="{DynamicResource TextPrimaryBrush}"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Column="2"
                    Visibility="Collapsed"
                    Background="{DynamicResource CardSurfaceBrush}"
                    BorderBrush="{DynamicResource BorderSubtleBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="14">
                        <TextBlock Text="Selected Process"
                                   FontSize="{StaticResource ProcessFontMedium}"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,8"/>

                        <Border Padding="10"
                                CornerRadius="{DynamicResource StandardCardCornerRadius}"
                                BorderBrush="{DynamicResource BorderSubtleBrush}"
                                BorderThickness="1"
                                Background="{DynamicResource QuietRowBackgroundBrush}"
                                Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="No process selected"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextWrapping="Wrap">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedProcess}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="Select a process from the table to enable affinity, priority, power plan, and rule actions."
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"
                                           Margin="0,4,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedProcess}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <StackPanel>
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedProcess}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Text="{Binding SelectedProcess.Name}"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock FontSize="{StaticResource ProcessFontSmall}" Foreground="{DynamicResource TextSecondaryBrush}">
                                        <Run Text="PID "/>
                                        <Run Text="{Binding SelectedProcess.ProcessId}"/>
                                        <Run Text="  Priority "/>
                                        <Run Text="{Binding SelectedProcess.Priority}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding SelectedProcess.MainWindowTitle, TargetNullValue=No visible window title}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <GroupBox Header="Affinity" Margin="0,0,0,12">
                            <StackPanel Margin="8">
                                <TextBlock Text="{Binding CurrentAffinityText}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"
                                           ToolTip="Affinity currently reported by Windows for the selected process"/>
                                <TextBlock Text="{Binding PendingAffinityText}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"
                                           Margin="0,3,0,0"
                                           ToolTip="Affinity staged in the UI. It is not applied until Apply Affinity is clicked."/>
                                <TextBlock Text="{Binding AffinityEditStateText}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Foreground="{DynamicResource WarningTextBrush}"
                                           TextWrapping="Wrap"
                                           Margin="0,3,0,10"/>

                                <TextBlock Text="Pending core mask" FontSize="{StaticResource ProcessFontSmall}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ComboBox ItemsSource="{Binding AvailableCoreMasks}"
                                          SelectedItem="{Binding SelectedCoreMask, Mode=TwoWay}"
                                          IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                          Margin="0,0,0,8"
                                          ToolTip="Select a mask to stage it. This does not change OS affinity until Apply Affinity is clicked.">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Margin="0,0,8,0"/>
                                                <TextBlock Text="{Binding SelectedCoreCount, StringFormat='({0} CPUs)'}"
                                                           FontSize="{StaticResource ProcessFontSmall}"
                                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                                           VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>

                                <TextBlock Text="{Binding TopologyStatus}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Foreground="{Binding IsTopologyDetectionSuccessful, Converter={StaticResource BoolToColorConverter}}"/>
                                <TextBlock Text="{Binding HyperThreadingStatusText}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Margin="0,2,0,6"
                                           Foreground="{Binding IsHyperThreadingActive, Converter={StaticResource BoolToColorConverter}}"
                                           ToolTip="Shows whether Hyper-Threading (Intel) or SMT (AMD) is present and active on this system"/>

                                <ScrollViewer MaxHeight="92"
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled"
                                              Margin="0,0,0,8">
                                    <ItemsControl ItemsSource="{Binding CpuCores}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox Content="{Binding Label}"
                                                          IsChecked="{Binding IsSelected, Mode=OneWay}"
                                                          IsHitTestVisible="False"
                                                          Focusable="False"
                                                          IsEnabled="{Binding IsEnabled}"
                                                          Margin="2"
                                                          FontSize="{StaticResource ProcessFontSmall}"
                                                          Foreground="{DynamicResource TextPrimaryBrush}"
                                                          ToolTip="Read-only preview of current or pending CPU selection"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0"
                                            Command="{Binding SetAffinityCommand}"
                                            Content="Apply Affinity"
                                            Padding="10,6"
                                            IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                            ToolTip="Apply the pending core mask to the selected process and verify the Windows affinity state"/>
                                    <Button Grid.Column="2"
                                            Command="{Binding CreateCustomMaskCommand}"
                                            Padding="8,6"
                                            ToolTip="Open CPU Masks tab to create a new custom mask">
                                        <StackPanel Orientation="Horizontal">
                                            <ui:SymbolIcon Symbol="Add12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Custom" FontSize="{StaticResource ProcessFontMediumSmall}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Priority" Margin="0,0,0,12">
                            <StackPanel Margin="8">
                                <ComboBox x:Name="PriorityComboBox"
                                          SelectedValue="{Binding SelectedProcess.Priority, Mode=TwoWay}"
                                          SelectedValuePath="Tag"
                                          Margin="0,0,0,6"
                                          IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}">
                                    <ComboBoxItem Content="Realtime" Tag="{x:Static Diagnostics:ProcessPriorityClass.RealTime}"/>
                                    <ComboBoxItem Content="High" Tag="{x:Static Diagnostics:ProcessPriorityClass.High}"/>
                                    <ComboBoxItem Content="Above Normal" Tag="{x:Static Diagnostics:ProcessPriorityClass.AboveNormal}"/>
                                    <ComboBoxItem Content="Normal" Tag="{x:Static Diagnostics:ProcessPriorityClass.Normal}"/>
                                    <ComboBoxItem Content="Below Normal" Tag="{x:Static Diagnostics:ProcessPriorityClass.BelowNormal}"/>
                                    <ComboBoxItem Content="Idle" Tag="{x:Static Diagnostics:ProcessPriorityClass.Idle}"/>
                                </ComboBox>
                                <Button Command="{Binding SetPriorityCommand}"
                                        CommandParameter="{Binding ElementName=PriorityComboBox, Path=SelectedValue}"
                                        Content="Set Priority"
                                        Padding="10,6"
                                        Margin="0,0,0,8"
                                        IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                        ToolTip="Apply the selected priority to the process"/>
                                <CheckBox Content="Enforce Priority by Registry"
                                          IsChecked="{Binding IsRegistryPriorityEnabled}"
                                          IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                          Margin="0,0,0,6"
                                          ToolTip="Apply priority through Windows registry. Process must be restarted for changes to take effect. This setting persists across system reboots."/>
                                <CheckBox Content="Disable Idle Server"
                                          IsChecked="{Binding IsIdleServerDisabled}"
                                          IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                          ToolTip="Prevents the system from entering idle state while this process is running. Useful for games and performance-critical applications."/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Power Plan / Pending Settings" Margin="0,0,0,12">
                            <StackPanel Margin="8">
                                <TextBlock Text="Selected power plan"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,4"/>
                                <ComboBox ItemsSource="{Binding PowerPlans}"
                                          SelectedItem="{Binding SelectedPowerPlan, Mode=TwoWay}"
                                          DisplayMemberPath="Name"
                                          Margin="0,0,0,8"
                                          ToolTip="Choose the power plan to activate manually or include with quick apply"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0"
                                            Command="{Binding SetPowerPlanCommand}"
                                            Content="Set Power Plan"
                                            Padding="10,6"
                                            ToolTip="Activate the selected Windows power plan"/>
                                    <Button Grid.Column="2"
                                            Command="{Binding QuickApplyAffinityAndPowerPlanCommand}"
                                            Content="Apply Pending Settings"
                                            Padding="10,6"
                                            IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                            ToolTip="Apply the pending affinity and selected power plan to the selected process"/>
                                </Grid>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Profile / Rule" Margin="0,0,0,12">
                            <StackPanel Margin="8">
                                <TextBox Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}"
                                         Margin="0,0,0,8"
                                         ToolTip="Enter a profile name to save or load settings"/>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0"
                                            Command="{Binding SaveProfileCommand}"
                                            Content="Save Profile"
                                            Padding="10,6"
                                            ToolTip="Save current CPU affinity and priority as a reusable profile"/>
                                    <Button Grid.Column="2"
                                            Command="{Binding LoadProfileCommand}"
                                            Content="Load Profile"
                                            Padding="10,6"
                                            ToolTip="Load a previously saved profile"/>
                                </Grid>
                                <Button Command="{Binding SaveCurrentAsAssociationCommand}"
                                        Content="Save as Rule"
                                        Padding="10,6"
                                        FontWeight="SemiBold"
                                        IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                        ToolTip="Save current process settings as a rule. These settings will be automatically applied when this process starts in the future."/>
                            </StackPanel>
                        </GroupBox>

                        <Border Padding="10,7"
                                CornerRadius="6"
                                BorderThickness="1"
                                Opacity="{Binding StatusOpacity}"
                                Visibility="{Binding StatusMessage, Converter={x:Static converters:StringToVisibilityConverter.Instance}}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{DynamicResource SuccessBackgroundBrush}"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource SuccessBorderBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasError}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource ErrorBackgroundBrush}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource ErrorBorderBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource SuccessTextBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasError}" Value="True">
                                                <Setter Property="Foreground" Value="{DynamicResource ErrorTextBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
