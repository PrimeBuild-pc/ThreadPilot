<UserControl x:Class="ThreadPilot.Views.ProcessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ThreadPilot.Converters"
             xmlns:Diagnostics="clr-namespace:System.Diagnostics;assembly=System.Diagnostics.Process"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             KeyboardNavigation.TabNavigation="Cycle"
             KeyboardNavigation.ControlTabNavigation="Cycle"
             d:DesignHeight="650" d:DesignWidth="1200">

    <UserControl.Resources>
        <converters:CoreTypeToColorConverter x:Key="CoreTypeToColorConverter"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:NullToBoolConverter x:Key="NullToBoolConverter"/>
        <converters:AffinityMaskConverter x:Key="AffinityMaskConverter"/>
        <converters:BytesToMbConverter x:Key="BytesToMbConverter"/>
        <sys:Double x:Key="ProcessFontLarge">24</sys:Double>
        <sys:Double x:Key="ProcessFontMedium">14</sys:Double>
        <sys:Double x:Key="ProcessFontMediumSmall">12</sys:Double>
        <sys:Double x:Key="ProcessFontSmall">11</sys:Double>
        <Style x:Key="ProcessContextMenuItemStyle" TargetType="{x:Type MenuItem}" BasedOn="{x:Null}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="MinWidth" Value="220"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="MenuItemRoot"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0"
                                SnapsToDevicePixels="True">
                            <Grid MinWidth="{TemplateBinding MinWidth}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0"
                                                  Margin="{TemplateBinding Padding}"
                                                  ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                                  TextElement.FontSize="{TemplateBinding FontSize}"
                                                  TextElement.FontWeight="{TemplateBinding FontWeight}"/>

                                <Path x:Name="SubmenuArrow"
                                      Grid.Column="1"
                                      Width="6"
                                      Height="10"
                                      Margin="8,0,12,0"
                                      Data="M 0 0 L 6 5 L 0 10 Z"
                                      Fill="{DynamicResource TextSecondaryBrush}"
                                      Stretch="Fill"
                                      VerticalAlignment="Center"
                                      Visibility="Collapsed"/>

                                <Popup x:Name="PART_Popup"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                       Placement="Right"
                                       PopupAnimation="Fade">
                                    <Border Background="{DynamicResource CardSurfaceBrush}"
                                            BorderBrush="{DynamicResource BorderSubtleBrush}"
                                            BorderThickness="1"
                                            Padding="4"
                                            SnapsToDevicePixels="True">
                                        <ScrollViewer CanContentScroll="True"
                                                      HorizontalScrollBarVisibility="Disabled"
                                                      VerticalScrollBarVisibility="Auto">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"
                                                            KeyboardNavigation.TabNavigation="Cycle"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="True">
                                <Setter TargetName="SubmenuArrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuItemRoot" Property="Background" Value="{DynamicResource QuietRowHoverBackgroundBrush}"/>
                                <Setter TargetName="SubmenuArrow" Property="Fill" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSubmenuOpen" Value="True">
                                <Setter TargetName="MenuItemRoot" Property="Background" Value="{DynamicResource QuietRowHoverBackgroundBrush}"/>
                                <Setter TargetName="SubmenuArrow" Property="Fill" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}"/>
                                <Setter TargetName="SubmenuArrow" Property="Fill" Value="{DynamicResource TextDisabledBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ProcessContextMenuSeparatorStyle" TargetType="{x:Type Separator}" BasedOn="{x:Null}">
            <Setter Property="Background" Value="{DynamicResource BorderSubtleBrush}"/>
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Separator}">
                        <Border Height="1"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ProcessContextMenuStyle" TargetType="{x:Type ContextMenu}" BasedOn="{x:Null}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource CardSurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"
                                            KeyboardNavigation.TabNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource ProcessView_Title}" Style="{StaticResource PageTitleTextStyle}" Margin="0,0,0,4"/>
            <TextBlock Text="{DynamicResource ProcessView_Subtitle}" Style="{StaticResource PageSubtitleTextStyle}"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0"/>
                <ColumnDefinition Width="0"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="{DynamicResource CardSurfaceBrush}"
                        Padding="{DynamicResource StandardCardPadding}"
                        BorderBrush="{DynamicResource BorderSubtleBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="{DynamicResource ProcessView_SearchPlaceholder}"
                                     HorizontalAlignment="Stretch"
                                     AutomationProperties.Name="{DynamicResource ProcessView_SearchAutomationName}"/>
                            <CheckBox Grid.Column="2"
                                      Content="{DynamicResource ProcessView_ActiveAppsOnly}"
                                      IsChecked="{Binding ShowActiveApplicationsOnly}"
                                      VerticalAlignment="Center"
                                      ToolTip="{DynamicResource ProcessView_VisibleWindowsOnly}"/>
                            <Button Grid.Column="4"
                                    Command="{Binding LoadProcessesCommand}"
                                    Content="{DynamicResource ProcessView_Refresh}"
                                    ToolTip="{DynamicResource ProcessView_RefreshTooltip}"
                                    AutomationProperties.Name="{DynamicResource ProcessView_RefreshAutomationName}"/>
                            <Button Grid.Column="6"
                                    Command="{Binding LoadMoreProcessesCommand}"
                                    Content="{DynamicResource ProcessView_LoadMore}"
                                    Visibility="{Binding HasMoreBatches, Converter={StaticResource BoolToVisibilityConverter}}"
                                    ToolTip="{DynamicResource ProcessView_LoadMoreTooltip}"
                                    AutomationProperties.Name="{DynamicResource ProcessView_LoadMoreAutomationName}"/>
                        </Grid>

                        <WrapPanel Grid.Row="2" Orientation="Horizontal">
                            <CheckBox Content="{DynamicResource ProcessView_HideSystemShort}"
                                      IsChecked="{Binding HideSystemProcesses}"
                                      Margin="0,0,15,0"
                                      VerticalAlignment="Center"
                                      ToolTip="{DynamicResource ProcessView_HideSystem}"/>
                            <CheckBox Content="{DynamicResource ProcessView_HideIdle}"
                                      IsChecked="{Binding HideIdleProcesses}"
                                      Margin="0,0,15,0"
                                      VerticalAlignment="Center"
                                      ToolTip="{DynamicResource ProcessView_HideLowCpu}"/>
                            <CheckBox Content="{DynamicResource ProcessView_LockList}"
                                      IsChecked="{Binding IsProcessListLocked}"
                                      Margin="0,0,15,0"
                                      VerticalAlignment="Center"
                                      ToolTip="{DynamicResource ProcessView_LockListTooltip}"/>
                            <TextBlock Text="{DynamicResource ProcessView_SortBy}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox SelectedValue="{Binding SortMode}"
                                      SelectedValuePath="Tag"
                                      Width="120"
                                      VerticalAlignment="Center"
                                      ToolTip="{DynamicResource ProcessView_SortByTooltip}">
                                <ComboBoxItem Content="{DynamicResource ProcessView_ColCpuUsage}" Tag="CpuUsage"/>
                                <ComboBoxItem Content="{DynamicResource ProcessView_ColMemory}" Tag="MemoryUsage"/>
                                <ComboBoxItem Content="{DynamicResource ProcessView_ColProcessName}" Tag="Name"/>
                                <ComboBoxItem Content="{DynamicResource ProcessView_ID}" Tag="ProcessId"/>
                            </ComboBox>
                        </WrapPanel>
                    </Grid>
                </Border>

                <Grid Grid.Row="1">
                    <DataGrid ItemsSource="{Binding FilteredProcesses}"
                              SelectedItem="{Binding SelectedProcess}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              CanUserResizeColumns="True"
                              CanUserSortColumns="True"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              VirtualizingPanel.ScrollUnit="Item"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              ScrollViewer.CanContentScroll="True"
                              AutomationProperties.Name="{DynamicResource ProcessView_RunningListAutomationName}"
                              AutomationProperties.HelpText="{DynamicResource ProcessView_RunningListAutomationHelp}">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="3,0,0,0"/>
                                <EventSetter Event="PreviewMouseRightButtonDown" Handler="ProcessRow_PreviewMouseRightButtonDown"/>
                                <Setter Property="ContextMenu">
                                    <Setter.Value>
                                        <ContextMenu Style="{StaticResource ProcessContextMenuStyle}"
                                                     DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                            <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                      Header="{DynamicResource ProcessView_ApplyAffinity}"
                                                      Command="{Binding ApplyContextAffinityCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                      Header="{DynamicResource ProcessView_ClearCpuSets}"
                                                      Command="{Binding ClearContextCpuSetsCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <Separator Style="{StaticResource ProcessContextMenuSeparatorStyle}"/>
                                            <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                      Header="{DynamicResource ProcessView_Rules}">
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_SaveCurrentRule}"
                                                          Command="{Binding SaveCurrentSettingsAsRuleCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_ApplyAffinitySaveRule}"
                                                          Command="{Binding ApplyAffinityAndSaveAsRuleCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            </MenuItem>
                                            <Separator Style="{StaticResource ProcessContextMenuSeparatorStyle}"/>
                                            <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                      Header="{DynamicResource ProcessView_SetCpuPriority}">
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_PriorityBelowNormal}"
                                                          Command="{Binding SetContextBelowNormalPriorityCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_PriorityNormal}"
                                                          Command="{Binding SetContextNormalPriorityCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_PriorityAboveNormal}"
                                                          Command="{Binding SetContextAboveNormalPriorityCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_PriorityHigh}"
                                                          Command="{Binding SetContextHighPriorityCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_RealtimeBlocked}"
                                                          IsEnabled="False"
                                                          ToolTipService.ShowOnDisabled="True"
                                                          ToolTip="{DynamicResource ProcessView_RealtimeBlockedHelp}"/>
                                            </MenuItem>
                                            <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                      Header="{DynamicResource ProcessView_SetMemoryPriority}">
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_PriorityVeryLow}"
                                                          Command="{Binding SetContextMemoryPriorityVeryLowCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_PriorityLow}"
                                                          Command="{Binding SetContextMemoryPriorityLowCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_PriorityMedium}"
                                                          Command="{Binding SetContextMemoryPriorityMediumCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_PriorityBelowNormal}"
                                                          Command="{Binding SetContextMemoryPriorityBelowNormalCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                          Header="{DynamicResource ProcessView_PriorityNormal}"
                                                          Command="{Binding SetContextMemoryPriorityNormalCommand}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            </MenuItem>
                                            <Separator Style="{StaticResource ProcessContextMenuSeparatorStyle}"/>
                                            <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                      Header="{DynamicResource ProcessView_OpenLocation}"
                                                      Command="{Binding OpenContextExecutableLocationCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                      Header="{DynamicResource ProcessView_CopyProcessInfo}"
                                                      Command="{Binding CopyContextProcessInfoCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <MenuItem Style="{StaticResource ProcessContextMenuItemStyle}"
                                                      Header="{DynamicResource ProcessView_RefreshInfo}"
                                                      Command="{Binding RefreshContextProcessInfoCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        </ContextMenu>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource QuietRowHoverBackgroundBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource SoftSelectionBackgroundBrush}"/>
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="True"/>
                                            <Condition Property="IsKeyboardFocusWithin" Value="True"/>
                                        </MultiTrigger.Conditions>
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentAltBrush}"/>
                                    </MultiTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource ProcessView_Name}" Binding="{Binding Name}" Width="160"/>
                            <DataGridTextColumn Header="{DynamicResource ProcessView_ID}" Binding="{Binding ProcessId}" Width="70"/>
                            <DataGridTextColumn Header="{DynamicResource ProcessView_ColWindowTitle}" Binding="{Binding MainWindowTitle}" Width="220"/>
                            <DataGridCheckBoxColumn Header="{DynamicResource ProcessView_HasWindow}" Binding="{Binding HasVisibleWindow}" Width="90"/>
                            <DataGridTextColumn Header="{DynamicResource ProcessView_ColCpuUsage}" Binding="{Binding CpuUsage, StringFormat={}{0:N1}%}" Width="90"/>
                            <DataGridTextColumn Header="{DynamicResource ProcessView_MemoryMb}"
                                                Binding="{Binding MemoryUsage, Converter={StaticResource BytesToMbConverter}}"
                                                Width="90"/>
                            <DataGridTextColumn Header="{DynamicResource ProcessView_Priority}" Binding="{Binding Priority}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Background="{DynamicResource OverlayBrush}" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <ProgressBar Value="{Binding LoadingProgress}" Maximum="100" Width="200" Height="20" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding LoadingStatusText}" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>

                    <Border Background="{DynamicResource StatusPillBackgroundBrush}"
                            BorderBrush="{DynamicResource StatusPillBorderBrush}"
                            BorderThickness="1"
                            Visibility="{Binding IsVirtualizationEnabled, Converter={StaticResource BoolToVisibilityConverter}}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Margin="10"
                            Padding="8,5"
                            CornerRadius="{DynamicResource StandardCardCornerRadius}">
                        <TextBlock FontSize="{StaticResource ProcessFontSmall}" Foreground="{DynamicResource TextSecondaryBrush}">
                            <Run Text="{DynamicResource ProcessView_BatchLabel}"/>
                            <Run Text="{Binding CurrentBatchIndex, StringFormat={}{0:N0}}"/>
                            <Run Text="/"/>
                            <Run Text="{Binding TotalBatches, StringFormat={}{0:N0}}"/>
                            <Run Text=" ("/>
                            <Run Text="{Binding TotalProcessCount, StringFormat={}{0:N0}}"/>
                            <Run Text="{DynamicResource ProcessView_TotalSuffix}"/>
                        </TextBlock>
                    </Border>
                </Grid>

                <Border Grid.Row="2"
                        Background="{DynamicResource CardSurfaceBrush}"
                        BorderBrush="{DynamicResource BorderSubtleBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="10"
                        Margin="0,12,0,0">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource ProcessView_CurrentProcessStatus}"
                                   FontSize="{StaticResource ProcessFontMediumSmall}"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,2"/>
                        <TextBlock Text="{DynamicResource ProcessView_RulesAppliedOnlyWhenConfigured}"
                                   FontSize="{StaticResource ProcessFontSmall}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,8"/>
                        <WrapPanel>
                            <TextBlock Text="{Binding SelectedProcessSummary.ProcessTitle}"
                                       FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis"
                                       Margin="0,0,18,4"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.CurrentProcessStatusText}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,4"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.CpuPriorityText}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,4"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.MemoryPriorityText}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,4"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.AffinityText}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,4"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.RuleStatusText}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,4"/>
                        </WrapPanel>
                        <TextBlock Text="{Binding SelectedProcessSummary.ExecutablePath}"
                                   FontSize="{StaticResource ProcessFontSmall}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   Margin="0,0,0,4"/>
                        <WrapPanel Margin="0,2,0,0">
                            <TextBlock Text="{Binding SelectedProcessSummary.CpuUsageText}"
                                       FontSize="{StaticResource ProcessFontSmall}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,0"/>
                            <TextBlock Text="{Binding SelectedProcessSummary.MemoryUsageText}"
                                       FontSize="{StaticResource ProcessFontSmall}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,18,0"/>
                            <TextBlock FontSize="{StaticResource ProcessFontSmall}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       TextWrapping="Wrap">
                                <Run Text="{DynamicResource ProcessView_LastAction}"/>
                                <Run Text="{Binding SelectedProcessSummary.LastOperationMessage, Mode=OneWay}"/>
                            </TextBlock>
                        </WrapPanel>

                        <Expander Header="{DynamicResource ProcessView_AdvancedAffinityPicker}"
                                  IsExpanded="False"
                                  Margin="0,10,0,0">
                            <StackPanel Margin="0,8,0,0">
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="260"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource ProcessView_PendingCoreMask}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"/>
                                    <ComboBox Grid.Column="2"
                                              ItemsSource="{Binding AvailableCoreMasks}"
                                              SelectedItem="{Binding SelectedCoreMask, Mode=TwoWay}"
                                              DisplayMemberPath="Name"
                                              IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                              ToolTip="{DynamicResource ProcessView_RowMaskTooltip}"/>
                                    <Button Grid.Column="4"
                                            Command="{Binding CreateCustomMaskCommand}"
                                            Padding="8,5"
                                            ToolTip="{DynamicResource ProcessView_OpenMasksTabTooltip}">
                                        <StackPanel Orientation="Horizontal">
                                            <ui:SymbolIcon Symbol="Add12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="{DynamicResource ProcessView_PriorityCustom}" FontSize="{StaticResource ProcessFontMediumSmall}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>

                                <WrapPanel Margin="0,0,0,8">
                                    <TextBlock Text="{Binding CurrentAffinityText}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,0,18,4"/>
                                    <TextBlock Text="{Binding PendingAffinityText}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,0,18,4"/>
                                    <TextBlock Text="{Binding AffinityEditStateText}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{DynamicResource WarningTextBrush}"
                                               Margin="0,0,18,4"/>
                                </WrapPanel>

                                <WrapPanel Margin="0,0,0,6">
                                    <TextBlock Text="{Binding TopologyStatus}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{Binding IsTopologyDetectionSuccessful, Converter={StaticResource BoolToColorConverter}}"
                                               Margin="0,0,18,4"/>
                                    <TextBlock Text="{Binding HyperThreadingStatusText}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{Binding IsHyperThreadingActive, Converter={StaticResource BoolToColorConverter}}"
                                               Margin="0,0,18,4"/>
                                </WrapPanel>

                                <ScrollViewer MaxHeight="88"
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding CpuCores}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox Content="{Binding Label}"
                                                          IsChecked="{Binding IsSelected, Mode=OneWay}"
                                                          IsHitTestVisible="False"
                                                          Focusable="False"
                                                          IsEnabled="{Binding IsEnabled}"
                                                          Margin="2"
                                                          FontSize="{StaticResource ProcessFontSmall}"
                                                          Foreground="{DynamicResource TextPrimaryBrush}"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Column="2"
                    Visibility="Collapsed"
                    Background="{DynamicResource CardSurfaceBrush}"
                    BorderBrush="{DynamicResource BorderSubtleBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="14">
                        <TextBlock Text="{DynamicResource ProcessView_SelectedProcess}"
                                   FontSize="{StaticResource ProcessFontMedium}"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,8"/>

                        <Border Padding="10"
                                CornerRadius="{DynamicResource StandardCardCornerRadius}"
                                BorderBrush="{DynamicResource BorderSubtleBrush}"
                                BorderThickness="1"
                                Background="{DynamicResource QuietRowBackgroundBrush}"
                                Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource ProcessView_NoProcessSelected}"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextWrapping="Wrap">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedProcess}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="{DynamicResource ProcessView_SelectProcessTip}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"
                                           Margin="0,4,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedProcess}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <StackPanel>
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedProcess}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Text="{Binding SelectedProcess.Name}"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock FontSize="{StaticResource ProcessFontSmall}" Foreground="{DynamicResource TextSecondaryBrush}">
                                        <Run Text="PID "/>
                                        <Run Text="{Binding SelectedProcess.ProcessId}"/>
                                        <Run Text="{DynamicResource ProcessView_PriorityInlineLabel}"/>
                                        <Run Text="{Binding SelectedProcess.Priority}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding SelectedProcess.MainWindowTitle, TargetNullValue=No visible window title}"
                                               FontSize="{StaticResource ProcessFontSmall}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <GroupBox Header="{DynamicResource ProcessView_Affinity}" Margin="0,0,0,12">
                            <StackPanel Margin="8">
                                <TextBlock Text="{Binding CurrentAffinityText}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"
                                           ToolTip="{DynamicResource ProcessView_AffinityCurrentTooltip}"/>
                                <TextBlock Text="{Binding PendingAffinityText}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"
                                           Margin="0,3,0,0"
                                           ToolTip="{DynamicResource ProcessView_AffinityStagedTooltip}"/>
                                <TextBlock Text="{Binding AffinityEditStateText}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Foreground="{DynamicResource WarningTextBrush}"
                                           TextWrapping="Wrap"
                                           Margin="0,3,0,10"/>

                                <TextBlock Text="{DynamicResource ProcessView_PendingCoreMask}" FontSize="{StaticResource ProcessFontSmall}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ComboBox ItemsSource="{Binding AvailableCoreMasks}"
                                          SelectedItem="{Binding SelectedCoreMask, Mode=TwoWay}"
                                          IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                          Margin="0,0,0,8"
                                          ToolTip="{DynamicResource ProcessView_StageMaskTooltip}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Margin="0,0,8,0"/>
                                                <TextBlock Text="{Binding SelectedCoreCount, StringFormat='({0} CPUs)'}"
                                                           FontSize="{StaticResource ProcessFontSmall}"
                                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                                           VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>

                                <TextBlock Text="{Binding TopologyStatus}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Foreground="{Binding IsTopologyDetectionSuccessful, Converter={StaticResource BoolToColorConverter}}"/>
                                <TextBlock Text="{Binding HyperThreadingStatusText}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           Margin="0,2,0,6"
                                           Foreground="{Binding IsHyperThreadingActive, Converter={StaticResource BoolToColorConverter}}"
                                           ToolTip="{DynamicResource ProcessView_HyperThreadingTooltip}"/>

                                <ScrollViewer MaxHeight="92"
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled"
                                              Margin="0,0,0,8">
                                    <ItemsControl ItemsSource="{Binding CpuCores}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox Content="{Binding Label}"
                                                          IsChecked="{Binding IsSelected, Mode=OneWay}"
                                                          IsHitTestVisible="False"
                                                          Focusable="False"
                                                          IsEnabled="{Binding IsEnabled}"
                                                          Margin="2"
                                                          FontSize="{StaticResource ProcessFontSmall}"
                                                          Foreground="{DynamicResource TextPrimaryBrush}"
                                                          ToolTip="{DynamicResource ProcessView_CpuSelectionPreviewTooltip}"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0"
                                            Command="{Binding SetAffinityCommand}"
                                            Content="{DynamicResource ProcessView_ApplyAffinity}"
                                            Padding="10,6"
                                            IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                            ToolTip="{DynamicResource ProcessView_ApplyAffinityTooltip}"/>
                                    <Button Grid.Column="2"
                                            Command="{Binding CreateCustomMaskCommand}"
                                            Padding="8,6"
                                            ToolTip="{DynamicResource ProcessView_OpenMasksTabTooltip}">
                                        <StackPanel Orientation="Horizontal">
                                            <ui:SymbolIcon Symbol="Add12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="{DynamicResource ProcessView_PriorityCustom}" FontSize="{StaticResource ProcessFontMediumSmall}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="{DynamicResource ProcessView_Priority}" Margin="0,0,0,12">
                            <StackPanel Margin="8">
                                <ComboBox x:Name="PriorityComboBox"
                                          SelectedValue="{Binding SelectedProcess.Priority, Mode=TwoWay}"
                                          SelectedValuePath="Tag"
                                          Margin="0,0,0,6"
                                          IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}">
                                    <ComboBoxItem Content="{DynamicResource ProcessView_PriorityRealtime}" Tag="{x:Static Diagnostics:ProcessPriorityClass.RealTime}"/>
                                    <ComboBoxItem Content="{DynamicResource ProcessView_PriorityHigh}" Tag="{x:Static Diagnostics:ProcessPriorityClass.High}"/>
                                    <ComboBoxItem Content="{DynamicResource ProcessView_PriorityAboveNormal}" Tag="{x:Static Diagnostics:ProcessPriorityClass.AboveNormal}"/>
                                    <ComboBoxItem Content="{DynamicResource ProcessView_PriorityNormal}" Tag="{x:Static Diagnostics:ProcessPriorityClass.Normal}"/>
                                    <ComboBoxItem Content="{DynamicResource ProcessView_PriorityBelowNormal}" Tag="{x:Static Diagnostics:ProcessPriorityClass.BelowNormal}"/>
                                    <ComboBoxItem Content="{DynamicResource ProcessView_PriorityIdle}" Tag="{x:Static Diagnostics:ProcessPriorityClass.Idle}"/>
                                </ComboBox>
                                <Button Command="{Binding SetPriorityCommand}"
                                        CommandParameter="{Binding ElementName=PriorityComboBox, Path=SelectedValue}"
                                        Content="{DynamicResource ProcessView_SetPriority}"
                                        Padding="10,6"
                                        Margin="0,0,0,8"
                                        IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                        ToolTip="{DynamicResource ProcessView_ApplyPriorityTooltip}"/>
                                <CheckBox Content="{DynamicResource ProcessView_EnforcePriorityRegistry}"
                                          IsChecked="{Binding IsRegistryPriorityEnabled}"
                                          IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                          Margin="0,0,0,6"
                                          ToolTip="{DynamicResource ProcessView_EnforcePriorityRegistryTooltip}"/>
                                <CheckBox Content="{DynamicResource ProcessView_DisableIdleServer}"
                                          IsChecked="{Binding IsIdleServerDisabled}"
                                          IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                          ToolTip="{DynamicResource ProcessView_DisableIdleServerTooltip}"/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="{DynamicResource ProcessView_PowerPlanPending}" Margin="0,0,0,12">
                            <StackPanel Margin="8">
                                <TextBlock Text="{DynamicResource ProcessView_SelectedPowerPlan}"
                                           FontSize="{StaticResource ProcessFontSmall}"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,4"/>
                                <ComboBox ItemsSource="{Binding PowerPlans}"
                                          SelectedItem="{Binding SelectedPowerPlan, Mode=TwoWay}"
                                          DisplayMemberPath="Name"
                                          Margin="0,0,0,8"
                                          ToolTip="{DynamicResource ProcessView_PowerPlanTooltip}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0"
                                            Command="{Binding SetPowerPlanCommand}"
                                            Content="{DynamicResource ProcessView_SetPowerPlan}"
                                            Padding="10,6"
                                            ToolTip="{DynamicResource ProcessView_SetPowerPlanTooltip}"/>
                                    <Button Grid.Column="2"
                                            Command="{Binding QuickApplyAffinityAndPowerPlanCommand}"
                                            Content="{DynamicResource ProcessView_ApplyPendingSettings}"
                                            Padding="10,6"
                                            IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                            ToolTip="{DynamicResource ProcessView_ApplyPendingSettingsTooltip}"/>
                                </Grid>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="{DynamicResource ProcessView_ProfileRule}" Margin="0,0,0,12">
                            <StackPanel Margin="8">
                                <TextBox Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}"
                                         Margin="0,0,0,8"
                                         ToolTip="{DynamicResource ProcessView_ProfileNamePlaceholder}"/>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0"
                                            Command="{Binding SaveProfileCommand}"
                                            Content="{DynamicResource ProcessView_SaveProfile}"
                                            Padding="10,6"
                                            ToolTip="{DynamicResource ProcessView_SaveProfileTooltip}"/>
                                    <Button Grid.Column="2"
                                            Command="{Binding LoadProfileCommand}"
                                            Content="{DynamicResource ProcessView_LoadProfile}"
                                            Padding="10,6"
                                            ToolTip="{DynamicResource ProcessView_LoadProfileTooltip}"/>
                                </Grid>
                                <Button Command="{Binding SaveCurrentAsAssociationCommand}"
                                        Content="{DynamicResource ProcessView_SaveAsRule}"
                                        Padding="10,6"
                                        FontWeight="SemiBold"
                                        IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                                        ToolTip="{DynamicResource ProcessView_ApplyAffinitySaveRuleTooltip}"/>
                            </StackPanel>
                        </GroupBox>

                        <Border Padding="10,7"
                                CornerRadius="6"
                                BorderThickness="1"
                                Opacity="{Binding StatusOpacity}"
                                Visibility="{Binding StatusMessage, Converter={x:Static converters:StringToVisibilityConverter.Instance}}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{DynamicResource SuccessBackgroundBrush}"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource SuccessBorderBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasError}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource ErrorBackgroundBrush}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource ErrorBorderBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource SuccessTextBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasError}" Value="True">
                                                <Setter Property="Foreground" Value="{DynamicResource ErrorTextBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
