<Window x:Class="ThreadPilot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ThreadPilot"
        xmlns:vm="clr-namespace:ThreadPilot.ViewModels"
        mc:Ignorable="d"
        Title="ThreadPilot - CPU Thread Optimization and Management"
        Height="750" Width="1200"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Top Menu -->
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_Import Profile..." Command="{Binding ImportProfileCommand}"/>
                <Separator/>
                <MenuItem Header="_Exit" Click="ExitMenuItem_Click"/>
            </MenuItem>
            <MenuItem Header="_Settings">
                <MenuItem Header="_Preferences..." Command="{Binding OpenSettingsCommand}"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About..." Command="{Binding OpenAboutCommand}"/>
            </MenuItem>
        </Menu>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Navigation Panel -->
            <Border Grid.Column="0" Background="{StaticResource BrushSidebarBackground}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Application Logo and Title -->
                    <StackPanel Grid.Row="0" Margin="15">
                        <TextBlock Text="ThreadPilot" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushPrimary}"/>
                        <TextBlock Text="Advanced Thread Management" FontSize="14" Foreground="{StaticResource BrushSecondary}" Margin="0,5,0,0"/>
                    </StackPanel>
                    
                    <!-- Navigation Items -->
                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding NavigationItems}" 
                             SelectedItem="{Binding SelectedNavigationItem}"
                             Background="Transparent"
                             BorderThickness="0"
                             Margin="10,20,10,10">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" Padding="10" FontSize="16"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    
                    <!-- Power Profiles Selector -->
                    <Border Grid.Row="2" Padding="15" Background="{StaticResource BrushSidebarAccent}">
                        <StackPanel>
                            <TextBlock Text="Power Profiles" FontWeight="Bold" Margin="0,0,0,10"/>
                            <ComboBox ItemsSource="{Binding PowerProfiles}"
                                      SelectedItem="{Binding SelectedPowerProfile}"
                                      DisplayMemberPath="Name"
                                      Margin="0,0,0,5"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
            
            <!-- Main Content Area -->
            <Border Grid.Column="1" Background="{StaticResource BrushContentBackground}" Padding="20">
                <ContentControl Content="{Binding CurrentView}"/>
            </Border>
        </Grid>
        
        <!-- Status Bar -->
        <Grid Grid.Row="2" Background="{StaticResource BrushStatusBarBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" Margin="10,5" Foreground="{StaticResource BrushStatusBarForeground}"/>
            <TextBlock Grid.Column="1" Text="{Binding SystemInfoText}" Margin="10,5" Foreground="{StaticResource BrushStatusBarForeground}"/>
        </Grid>
    </Grid>
</Window>